Buying [<%= @quote.symbol %>]<br>
Price $<%= @quote.latest_price %><br>
<% if @stock.present? %>
  <% if @stock.stock_quantity > 0 %>
    Stock: <%= @stock.stock_quantity %><br>
  <% end %>
<% end %>
<br>
<% if @stock.present? %>
  <%= render 'form', quote: @quote, symbol_id: @stock.stock_id %>
<% else %>
  <%= render 'form', quote: @quote, symbol_id: @stock_id %>
<% end %>
