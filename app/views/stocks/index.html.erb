<% @stocks.each do |symbol| %>
  <% quote = @client.quote(symbol.sym) %>
  Symbol: <%= symbol.sym %><br>
  Company name: <%= quote.company_name %><br>
  $<%= quote.latest_price %><br>
  <%= link_to 'buy', transactions_buy_path(SS: symbol.sym, SID: symbol.id) %>
  <br>
  <br>
<% end %>
